import { describe, expect, it } from 'vitest'; import { createApp } from '../src/app.js'; import { encryptToken, decryptToken } from '../src/security/encryption.js';
describe('account foundation',()=>{ it('encrypts and authenticates Apple tokens',()=>{const key=Buffer.alloc(32,7);const value=encryptToken('refresh-secret',key);expect(decryptToken(value,key)).toBe('refresh-secret');const parts=value.split('.');parts[2]+='x';expect(()=>decryptToken(parts.join('.'),key)).toThrow();}); it('health is intentionally minimal',async()=>{const app=createApp();const r=await app.inject({method:'GET',url:'/health'});expect(r.statusCode).toBe(200);expect(r.json()).toEqual({ok:true});}); it('challenge returns random client inputs without token hashes',async()=>{const app=createApp();const r=await app.inject({method:'POST',url:'/v1/auth/apple/challenge',payload:{installationId:'00000000-0000-4000-8000-000000000000'}});expect(r.statusCode).toBe(200);expect(r.json()).not.toHaveProperty('nonceHash');});});
