# Recognition fixtures

Keep OCR text synthetic and privacy-safe. Use `RecognitionFixtureSanitizer` on copied OCR, then replace tracking markers with format-valid synthetic values; leave courier names and pickup codes unchanged. Run with `flutter test test/fixtures/recognition`.
